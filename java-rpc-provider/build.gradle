group 'indi.yoalone'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile(project(':java-rpc-core'))
    compile(project(':java-rpc-api'))
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
